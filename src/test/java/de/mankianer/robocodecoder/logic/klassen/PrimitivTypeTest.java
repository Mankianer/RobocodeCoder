package de.mankianer.robocodecoder.logic.klassen;

import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

@RunWith(SpringRunner.class)
@SpringBootTest
public class PrimitivTypeTest {

    @Test
    public void typeIsSubtypeTest() {
//        PrimitivType type = PrimitivType.createType("Test", PrimitivType.SubType.INT);
//        System.out.println(type);
//
//        Assert.assertTrue(type.isSubType(PrimitivType.INT));
//        Assert.assertFalse(type.isSubType(PrimitivType.STRING));
//        Assert.assertFalse(PrimitivType.INT.isSubType(type));
//        Assert.assertFalse(PrimitivType.STRING.isSubType(type));
//        Assert.assertTrue(type.isSubType(type));
    }
}
